{
  "id": "1.9",
  "title": "Loops",
  "type": "coding",
  "phase1": {
    "code": "using UnityEngine;\n\npublic class Loops : MonoBehaviour\n{\n    void Start()\n    {\n        // for loop — spawn 5 enemies\n        Debug.Log(\"=== Spawning Enemies ===\");\n        for (int i = 0; i < 5; i++)\n        {\n            Debug.Log(\"Spawned enemy #\" + (i + 1));\n        }\n        \n        // while loop — countdown\n        Debug.Log(\"\\n=== Countdown ===\");\n        int countdown = 5;\n        while (countdown > 0)\n        {\n            Debug.Log(countdown + \"...\");\n            countdown--;\n        }\n        Debug.Log(\"GO!\");\n        \n        // foreach loop — iterate over an array\n        Debug.Log(\"\\n=== Inventory ===\");\n        string[] items = { \"Sword\", \"Shield\", \"Potion\", \"Key\" };\n        foreach (string item in items)\n        {\n            Debug.Log(\"Item: \" + item);\n        }\n        \n        // do-while — runs at least once\n        Debug.Log(\"\\n=== Do-While ===\");\n        int attempts = 0;\n        do\n        {\n            Debug.Log(\"Attempt \" + (attempts + 1));\n            attempts++;\n        } while (attempts < 3);\n        \n        // break and continue\n        Debug.Log(\"\\n=== Break and Continue ===\");\n        for (int i = 0; i < 10; i++)\n        {\n            if (i == 3)\n            {\n                continue;  // Skip 3\n            }\n            if (i == 7)\n            {\n                break;     // Stop at 7\n            }\n            Debug.Log(\"Number: \" + i);\n        }\n    }\n}",
    "output": "=== Spawning Enemies ===\nSpawned enemy #1\nSpawned enemy #2\nSpawned enemy #3\nSpawned enemy #4\nSpawned enemy #5\n\n=== Countdown ===\n5...\n4...\n3...\n2...\n1...\nGO!\n\n=== Inventory ===\nItem: Sword\nItem: Shield\nItem: Potion\nItem: Key\n\n=== Do-While ===\nAttempt 1\nAttempt 2\nAttempt 3\n\n=== Break and Continue ===\nNumber: 0\nNumber: 1\nNumber: 2\nNumber: 4\nNumber: 5\nNumber: 6",
    "explanation": "🎮 **Loops repeat code multiple times.**\n\n**`for` loop** — when you know how many times to repeat:\n```csharp\nfor (int i = 0; i < 5; i++)\n{\n    // Runs 5 times\n}\n```\n\n**`while` loop** — when you need to loop until a condition changes:\n```csharp\nwhile (health > 0)\n{\n    // Runs while health is above 0\n}\n```\n\n**`foreach` loop** — iterating over collections:\n```csharp\nforeach (string item in inventory)\n{\n    // Runs once per item\n}\n```\n\n**`do-while` loop** — always runs at least once:\n```csharp\ndo\n{\n    // Runs at least once\n} while (condition);\n```\n\n**Loop control:**\n| Keyword | What it does |\n|---------|--------------|\n| `break` | Exits the loop immediately |\n| `continue` | Skips the rest of this iteration |\n\n**Sharp-chan says:**\n\"Loops are how you spawn enemies, process inventory, and handle countdowns. They're essential.\"",
    "openingDialogue": "Loops: repeat code efficiently. 🔄"
  },
  "phase2": {
    "brokenCode": "using UnityEngine;\n\npublic class LoopError : MonoBehaviour\n{\n    void Start()\n    {\n        // Infinite loop! No increment condition\n        int i = 0;\n        while (i < 10)\n        {\n            Debug.Log(\"Looping...\");\n            // i++; // Missing increment!\n        }\n    }\n}",
    "errorMessage": "(The game freezes or runs forever! Unity's editor will eventually freeze, or you'll get a timeout.)",
    "explanation": "Without changing `i`, the condition `i < 10` is always true. The loop never ends.\n\n**The fix:**\n```csharp\nint i = 0;\nwhile (i < 10)\n{\n    Debug.Log(\"Looping...\");\n    i++;  // ← Increment is required!\n}\n```\n\n**Sharp-chan says:**\n\"Always check: 'What changes in this loop?' If nothing changes, it's an infinite loop. Unity will freeze. Always test your loops.\""
  },
  "phase3": {
    "prompt": "Which loop would you use to iterate over all items in a collection (like an array)?\n\nA) for loop\nB) while loop\nC) foreach loop\nD) do-while loop\n\nSelect the CORRECT answer.",
    "validationPattern": {
      "mcqAnswer": "C"
    },
    "dialogueHints": [
      "Think about which loop is specifically designed for collections.",
      "It's cleaner than using a for loop with an index.",
      "It's the most readable option."
    ],
    "solution": null,
    "openingDialogue": "Loop check! Which one is for collections? 🤔"
  },
  "xpReward": 10
}