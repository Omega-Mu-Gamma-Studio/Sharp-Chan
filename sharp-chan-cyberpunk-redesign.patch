diff --git a/src/components/character/SharpChan.css b/src/components/character/SharpChan.css
index bbb81de..3991fc4 100644
--- a/src/components/character/SharpChan.css
+++ b/src/components/character/SharpChan.css
@@ -103,18 +103,18 @@
   align-items: center;
   justify-content: center;
   cursor: pointer;
-  /* Dark bg with orange tint to match her palette */
-  background: rgba(10, 8, 5, 0.93);
+  /* Near-black with a neon magenta/cyan tint to match her palette */
+  background: rgba(6, 4, 12, 0.94);
 }
 
-/* Radial orange-pink glow in center */
+/* Radial magenta-cyan glow in center */
 .domain-glow {
   position: absolute;
   inset: 0;
   background:
     radial-gradient(ellipse 60% 50% at 50% 60%,
-      rgba(255, 120, 20, 0.18) 0%,
-      rgba(255, 80, 160, 0.08) 50%,
+      rgba(255, 46, 203, 0.2) 0%,
+      rgba(100, 210, 255, 0.1) 50%,
       transparent 80%);
   pointer-events: none;
 }
@@ -143,8 +143,8 @@
   height: 100%;
   object-fit: contain;
   object-position: bottom center;
-  filter: drop-shadow(0 0 40px rgba(255, 140, 0, 0.5))
-          drop-shadow(0 0 80px rgba(255, 80, 160, 0.2));
+  filter: drop-shadow(0 0 40px rgba(255, 46, 203, 0.45))
+          drop-shadow(0 0 80px rgba(100, 210, 255, 0.25));
 }
 
 .domain-dialogue {
@@ -152,17 +152,17 @@
   font-size: 1.05rem;
   line-height: 1.75;
   color: var(--color-text);
-  background: rgba(20, 16, 10, 0.85);
-  border: 1px solid rgba(255, 140, 0, 0.25);
+  background: rgba(14, 10, 20, 0.85);
+  border: 1px solid rgba(255, 46, 203, 0.25);
   border-radius: var(--radius-lg);
   padding: 18px 26px;
   backdrop-filter: blur(8px);
-  box-shadow: 0 0 30px rgba(255,120,20,0.1);
+  box-shadow: 0 0 30px rgba(255, 46, 203, 0.1);
 }
 
 .domain-tap-hint {
   font-size: 0.72rem;
-  color: rgba(255, 200, 120, 0.4);
+  color: rgba(180, 220, 255, 0.5);
   letter-spacing: 0.12em;
   text-transform: uppercase;
   animation: domain-blink 1.8s ease-in-out infinite;
diff --git a/src/components/layout/AnimatedBg.css b/src/components/layout/AnimatedBg.css
index bcb6bb1..afb66c4 100644
--- a/src/components/layout/AnimatedBg.css
+++ b/src/components/layout/AnimatedBg.css
@@ -41,7 +41,7 @@
 .aurora__blob--3 {
   width: 450px;
   height: 450px;
-  background: radial-gradient(circle, rgba(180, 110, 255, 0.4) 0%, transparent 70%);
+  background: radial-gradient(circle, rgba(255, 46, 203, 0.32) 0%, transparent 70%);
   bottom: -10%;
   left: 25%;
   animation: aurora-drift-3 18s ease-in-out infinite alternate;
diff --git a/src/components/layout/AppLayout.css b/src/components/layout/AppLayout.css
index 5880909..1369ae9 100644
--- a/src/components/layout/AppLayout.css
+++ b/src/components/layout/AppLayout.css
@@ -34,12 +34,12 @@
   height: 1px;
   background: linear-gradient(90deg,
     transparent,
+    var(--color-magenta),
     var(--color-primary),
     var(--color-cyan),
-    var(--color-primary),
     transparent
   );
-  opacity: 0.4;
+  opacity: 0.45;
 }
 
 .hamburger-btn {
@@ -85,7 +85,7 @@
 }
 
 .topbar-title-plus {
-  color: var(--color-cyan);
+  color: var(--color-magenta);
   font-size: 1.1rem;
 }
 
diff --git a/src/components/ui/SpotlightCard.jsx b/src/components/ui/SpotlightCard.jsx
new file mode 100644
index 0000000..2d5e1d5
--- /dev/null
+++ b/src/components/ui/SpotlightCard.jsx
@@ -0,0 +1,48 @@
+import { useRef, useCallback } from 'react';
+
+/**
+ * SpotlightCard
+ *
+ * Wraps any card content with a cursor-reactive neon glow.
+ * Pairs with the `.spotlight` utility class in globals.css —
+ * this component just handles the pointer tracking, the CSS
+ * handles the actual gradient rendering.
+ *
+ * Usage:
+ *   <SpotlightCard className="unit-card unit-card--active">
+ *     ...card content...
+ *   </SpotlightCard>
+ */
+const SpotlightCard = ({ as: Tag = 'div', className = '', children, style, ...rest }) => {
+  const ref = useRef(null);
+
+  const handleMove = useCallback((e) => {
+    const el = ref.current;
+    if (!el) return;
+    const rect = el.getBoundingClientRect();
+    el.style.setProperty('--spot-x', `${e.clientX - rect.left}px`);
+    el.style.setProperty('--spot-y', `${e.clientY - rect.top}px`);
+    el.style.setProperty('--spot-opacity', '1');
+  }, []);
+
+  const handleLeave = useCallback(() => {
+    const el = ref.current;
+    if (!el) return;
+    el.style.setProperty('--spot-opacity', '0');
+  }, []);
+
+  return (
+    <Tag
+      ref={ref}
+      className={`spotlight ${className}`}
+      style={style}
+      onMouseMove={handleMove}
+      onMouseLeave={handleLeave}
+      {...rest}
+    >
+      {children}
+    </Tag>
+  );
+};
+
+export default SpotlightCard;
diff --git a/src/data/shopItems.js b/src/data/shopItems.js
index 8f0e1d5..aea0315 100644
--- a/src/data/shopItems.js
+++ b/src/data/shopItems.js
@@ -2,14 +2,14 @@ export const SHOP_ITEMS = [
   {
     id: 'wallpaper-default',
     type: 'wallpaper',
-    name: 'Midnight Violet',
+    name: 'Neon Circuit',
     requiredLevel: 1,
     emoji: '🌌',
     accent: '#7c6fff',
     themeClass: '',
-    gradient: 'linear-gradient(135deg, #07050f 0%, #0e0b1a 100%)',
+    gradient: 'linear-gradient(135deg, #ff2ecb 0%, #7c6fff 55%, #64d2ff 100%)',
     imageSrc: null,
-    description: 'The default. Cold, precise, and deeply purple.',
+    description: 'The default. Violet base, magenta-to-cyan current running through it.',
   },
   {
     id: 'outfit-default',
@@ -55,14 +55,14 @@ export const SHOP_ITEMS = [
   {
     id: 'wallpaper-steel',
     type: 'wallpaper',
-    name: 'Iron Throne',
+    name: 'Chrome District',
     requiredLevel: 3,
     emoji: '⚙️',
-    accent: '#5c7a94',
+    accent: '#6ee7ff',
     themeClass: 'theme-steel',
-    gradient: 'linear-gradient(160deg, #080c10 0%, #0d1520 55%, #111a22 100%)',
+    gradient: 'linear-gradient(160deg, #6ee7ff 0%, #8fb8ff 55%, #b8f4ff 100%)',
     imageSrc: null,
-    description: 'Gunmetal grey, deep crimson, abyssal blue eyes. The borrow checker\'s throne.',
+    description: 'Cool chrome, glass towers, cyan skyline. Late-night server room energy.',
   },
   {
     id: 'outfit-competitive-programmer',
@@ -87,14 +87,14 @@ export const SHOP_ITEMS = [
   {
     id: 'wallpaper-indigo-gold',
     type: 'wallpaper',
-    name: 'Stormcaller',
+    name: 'Glitch Gold',
     requiredLevel: 5,
     emoji: '👑',
-    accent: '#5b4a8c',
+    accent: '#ffcc4d',
     themeClass: 'theme-indigo-gold',
-    gradient: 'linear-gradient(160deg, #08060f 0%, #130d28 55%, #1a1030 100%)',
+    gradient: 'linear-gradient(160deg, #ff4fd8 0%, #ffcc4d 55%, #fff1b8 100%)',
     imageSrc: null,
-    description: 'Storm grey, charcoal, blazing orange lightning. The storm unleashed.',
+    description: 'Overclocked gold circuitry with a magenta glitch running through it.',
   },
   {
     id: 'outfit-Witch',
@@ -122,11 +122,11 @@ export const SHOP_ITEMS = [
     name: 'Neon Alley',
     requiredLevel: 7,
     emoji: '🔴',
-    accent: '#d6203f',
+    accent: '#ff2e6e',
     themeClass: 'theme-crimson',
-    gradient: 'linear-gradient(160deg, #0f0508 0%, #1e0810 55%, #150510 100%)',
+    gradient: 'linear-gradient(160deg, #ff2ecb 0%, #ff2e6e 55%, #ffb347 100%)',
     imageSrc: null,
-    description: 'Deep crimson, black, neon orange. A rain-slicked alley at 2 a.m.',
+    description: 'Hot pink and red neon signage. A rain-slicked alley at 2 a.m.',
   },
   {
     id: 'outfit-military',
@@ -154,11 +154,11 @@ export const SHOP_ITEMS = [
     name: 'Compiled',
     requiredLevel: 9,
     emoji: '💻',
-    accent: '#39d97a',
+    accent: '#39ff8f',
     themeClass: 'theme-terminal',
-    gradient: 'linear-gradient(160deg, #050f08 0%, #081a0e 55%, #050f08 100%)',
+    gradient: 'linear-gradient(160deg, #64d2ff 0%, #39ff8f 55%, #d4ffe8 100%)',
     imageSrc: null,
-    description: 'Cool blue, terminal green, matte black. The channel room, no warnings.',
+    description: 'Cyan and matrix green. The channel room, zero warnings, build passing.',
   },
   {
     id: 'outfit-legendary',
diff --git a/src/pages/Home.css b/src/pages/Home.css
index 18d8922..ab4b4a8 100644
--- a/src/pages/Home.css
+++ b/src/pages/Home.css
@@ -1,9 +1,66 @@
 .home-page {
   padding: 28px 32px 40px;
+  max-width: 1180px;
+}
+
+.home-grid {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) 300px;
+  gap: 28px;
+  align-items: start;
+}
+
+.home-main {
   display: flex;
   flex-direction: column;
   gap: 32px;
-  max-width: 800px;
+  min-width: 0;
+}
+
+/* ── HERO ── */
+.hero-panel {
+  position: relative;
+  overflow: hidden;
+  border: 1px solid var(--color-border-accent);
+  border-radius: var(--radius-xl);
+  padding: 28px 32px;
+  background: var(--color-surface-2);
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+}
+
+.hero-glow {
+  position: absolute;
+  inset: 0;
+  background: var(--gradient-hero);
+  pointer-events: none;
+}
+
+.hero-kicker {
+  position: relative;
+  font-family: var(--font-code);
+  font-size: 0.7rem;
+  letter-spacing: 0.14em;
+  color: var(--color-cyan);
+  opacity: 0.8;
+}
+
+.hero-title {
+  position: relative;
+  font-family: var(--font-display);
+  font-size: 1.9rem;
+  font-weight: 900;
+  letter-spacing: 0.01em;
+  color: var(--color-text);
+  margin-top: 6px;
+}
+
+.hero-sub {
+  position: relative;
+  color: var(--color-text-muted);
+  font-size: 0.9rem;
+  margin-bottom: 18px;
 }
 
 /* ── STAT ROW ── */
@@ -32,21 +89,21 @@
   position: absolute;
   top: 0; left: 0; right: 0;
   height: 2px;
-  background: linear-gradient(90deg, var(--color-primary), var(--color-cyan));
-  opacity: 0.7;
+  background: var(--gradient-neon);
+  opacity: 0.75;
 }
 
 .stat-card:hover {
   border-color: var(--color-primary);
-  box-shadow: 0 0 20px var(--color-primary-glow);
+  box-shadow: 0 0 24px var(--color-primary-glow);
 }
 
 .stat-card--xp .stat-icon {
   position: absolute;
   top: 14px; right: 16px;
   font-size: 1.4rem;
-  color: var(--color-primary);
-  opacity: 0.3;
+  color: var(--color-magenta);
+  opacity: 0.35;
   animation: xp-pulse 2.5s ease-in-out infinite;
 }
 
@@ -112,7 +169,7 @@
 
 .stat-bar-fill {
   height: 100%;
-  background: linear-gradient(90deg, var(--color-primary-dark), var(--color-primary));
+  background: var(--gradient-neon);
   border-radius: var(--radius-full);
   transition: width 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
   box-shadow: 0 0 6px var(--color-primary-glow);
@@ -130,6 +187,7 @@
 
 /* ── CTA ── */
 .home-cta {
+  position: relative;
   display: flex;
   flex-direction: column;
   gap: 10px;
@@ -151,17 +209,17 @@
 }
 
 .cta-btn--primary {
-  background: linear-gradient(135deg, var(--color-primary-dark), var(--color-primary), var(--color-cyan));
+  background: var(--gradient-neon);
   background-size: 200% 200%;
   background-position: 0% 50%;
   color: white;
-  box-shadow: 0 4px 24px var(--color-primary-glow), 0 0 0 1px rgba(124,111,255,0.3);
+  box-shadow: 0 4px 24px var(--color-magenta-glow), 0 0 0 1px rgba(124,111,255,0.3);
 }
 
 .cta-btn--primary:hover {
   background-position: 100% 50%;
   transform: translateY(-2px);
-  box-shadow: 0 8px 32px var(--color-primary-glow), 0 0 0 1px var(--color-primary);
+  box-shadow: 0 8px 36px var(--color-magenta-glow), 0 0 0 1px var(--color-primary);
 }
 
 .cta-btn--primary:active {
@@ -196,14 +254,14 @@
   display: inline-block;
   width: 3px;
   height: 14px;
-  background: linear-gradient(to bottom, var(--color-primary), var(--color-cyan));
+  background: var(--gradient-neon);
   border-radius: 2px;
 }
 
 /* ── UNIT GRID ── */
 .unit-grid {
   display: grid;
-  grid-template-columns: repeat(5, 1fr);
+  grid-template-columns: repeat(3, 1fr);
   gap: 10px;
 }
 
@@ -286,12 +344,151 @@
 
 .unit-card-bar-fill {
   height: 100%;
-  background: linear-gradient(90deg, var(--color-primary), var(--color-cyan));
+  background: var(--gradient-neon);
   border-radius: var(--radius-full);
   transition: width 1s ease;
 }
 
+/* ══════════════ SIBLINGS PANEL ══════════════ */
+.siblings-panel {
+  position: sticky;
+  top: 0;
+  background: rgba(14, 11, 26, 0.6);
+  border: 1px solid var(--color-border-accent);
+  border-radius: var(--radius-lg);
+  padding: 20px 18px;
+  backdrop-filter: blur(8px);
+}
+
+.siblings-intro {
+  font-size: 0.76rem;
+  color: var(--color-text-faint);
+  margin: -6px 0 16px;
+  line-height: 1.5;
+}
+
+.siblings-list {
+  display: flex;
+  flex-direction: column;
+  gap: 8px;
+}
+
+.sibling-card {
+  --sib-hue: var(--color-primary);
+  position: relative;
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  padding: 10px 12px;
+  border: 1px solid var(--color-border);
+  border-radius: var(--radius-md);
+  text-decoration: none;
+  color: var(--color-text);
+  background: rgba(255, 255, 255, 0.015);
+  transition: all var(--transition-fast);
+  overflow: hidden;
+}
+
+.sibling-card::before {
+  content: '';
+  position: absolute;
+  top: 0; left: 0; bottom: 0;
+  width: 2px;
+  background: var(--sib-hue);
+  opacity: 0.6;
+  transition: opacity var(--transition-fast);
+}
+
+.sibling-card:hover {
+  border-color: var(--sib-hue);
+  background: color-mix(in srgb, var(--sib-hue) 10%, transparent);
+  box-shadow: 0 0 16px color-mix(in srgb, var(--sib-hue) 35%, transparent);
+  transform: translateX(2px);
+}
+
+.sibling-card:hover::before {
+  opacity: 1;
+}
+
+.sibling-card--self {
+  cursor: default;
+  border-style: dashed;
+  border-color: color-mix(in srgb, var(--sib-hue) 45%, transparent);
+  background: color-mix(in srgb, var(--sib-hue) 8%, transparent);
+}
+
+.sibling-card--self:hover {
+  transform: none;
+}
+
+.sibling-glyph {
+  width: 32px;
+  height: 32px;
+  flex-shrink: 0;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-size: 1rem;
+  border-radius: var(--radius-sm);
+  background: color-mix(in srgb, var(--sib-hue) 18%, transparent);
+  color: var(--sib-hue);
+}
+
+.sibling-info {
+  display: flex;
+  flex-direction: column;
+  gap: 1px;
+  min-width: 0;
+  flex: 1;
+}
+
+.sibling-name {
+  font-size: 0.82rem;
+  font-weight: 600;
+  color: var(--color-text);
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+
+.sibling-lang {
+  font-size: 0.66rem;
+  color: var(--color-text-faint);
+  font-family: var(--font-code);
+}
+
+.sibling-arrow {
+  font-size: 0.85rem;
+  color: var(--sib-hue);
+  opacity: 0.7;
+  flex-shrink: 0;
+}
+
+.sibling-tag {
+  font-size: 0.6rem;
+  text-transform: uppercase;
+  letter-spacing: 0.06em;
+  color: var(--sib-hue);
+  border: 1px solid color-mix(in srgb, var(--sib-hue) 50%, transparent);
+  border-radius: var(--radius-full);
+  padding: 3px 8px;
+  flex-shrink: 0;
+  white-space: nowrap;
+}
+
 /* ── RESPONSIVE ── */
+@media (max-width: 980px) {
+  .home-grid {
+    grid-template-columns: 1fr;
+  }
+  .siblings-panel {
+    position: static;
+  }
+  .unit-grid {
+    grid-template-columns: repeat(2, 1fr);
+  }
+}
+
 @media (max-width: 700px) {
   .home-stats {
     grid-template-columns: 1fr 1fr;
@@ -299,4 +496,7 @@
   .unit-grid {
     grid-template-columns: 1fr 1fr;
   }
-}
\ No newline at end of file
+  .hero-title {
+    font-size: 1.5rem;
+  }
+}
diff --git a/src/pages/Home.jsx b/src/pages/Home.jsx
index 8a49849..9f94839 100644
--- a/src/pages/Home.jsx
+++ b/src/pages/Home.jsx
@@ -1,15 +1,26 @@
 import { useEffect } from 'react';
 import { useNavigate } from 'react-router-dom';
+import { motion } from 'framer-motion';
 import { useProgress } from '../hooks/useProgress';
 import useLessonStore from '../store/lessonStore';
+import SpotlightCard from '../components/ui/SpotlightCard';
 import './Home.css';
 
 const UNITS = [
-  { id: 1, title: 'C# and Unity Foundations',                   icon: '⬡', lessons: 15 },
-  { id: 2, title: 'OOP and Unity Scripting',               icon: '⬡', lessons: 15 },
-  { id: 3, title: '2D Game Systems',                 icon: '⬡', lessons: 15 },
-  { id: 4, title: 'Polish and Advanced C#',                   icon: '⬡', lessons: 15 },
-  { id: 5, title: 'Final Game Project',       icon: '⬡', lessons: 15 },
+  { id: 1, title: 'C# and Unity Foundations', icon: '⬡', lessons: 15 },
+  { id: 2, title: 'OOP and Unity Scripting',   icon: '⬡', lessons: 15 },
+  { id: 3, title: '2D Game Systems',           icon: '⬡', lessons: 15 },
+  { id: 4, title: 'Polish and Advanced C#',    icon: '⬡', lessons: 15 },
+  { id: 5, title: 'Final Game Project',        icon: '⬡', lessons: 15 },
+];
+
+const SIBLINGS = [
+  { name: 'Java-chan',      lang: 'Java',   glyph: '☕', href: 'https://java-chan.vercel.app',     hue: '#ff8a3d' },
+  { name: 'PlusPlus-chan',  lang: 'C++',    glyph: '➕', href: 'https://plusplus-chan.vercel.app', hue: '#6e9bff' },
+  { name: 'Rust-chan',      lang: 'Rust',   glyph: '⚙',  href: 'https://rust-chan.vercel.app',     hue: '#ff6e4a' },
+  { name: 'Go-chan',        lang: 'Go',     glyph: '◈',  href: 'https://go-chan.vercel.app',       hue: '#4ad7ff' },
+  { name: 'Kotlin-chan',    lang: 'Kotlin', glyph: '◆',  href: 'https://kotlin-chan.vercel.app',   hue: '#b46eff' },
+  { name: 'Sharp-chan',     lang: 'C#',     glyph: '♯',  href: 'https://sharp-chan.vercel.app',    hue: '#ff2ecb', self: true },
 ];
 
 const Home = () => {
@@ -24,131 +35,178 @@ const Home = () => {
   useEffect(() => {
     setExpression('idle');
     const greetings = [
-  "You're back! I was just playtesting your progress.",
-  "I've been waiting. The sprites are getting lonely.",
-  "Ready to build something amazing? I thought so.",
-  "You're here. Good. I was about to start without you. ...Kidding. Mostly.",
-  "I've been sketching ideas. You're going to love this one.",
-  "Welcome back! I saved you a seat — and a bug to fix.",
-  "You're right on time. I was just about to ship something.",
-  ];
+      "You're back! I was just playtesting your progress.",
+      "I've been waiting. The sprites are getting lonely.",
+      "Ready to build something amazing? I thought so.",
+      "You're here. Good. I was about to start without you. ...Kidding. Mostly.",
+      "I've been sketching ideas. You're going to love this one.",
+      "Welcome back! I saved you a seat — and a bug to fix.",
+      "You're right on time. I was just about to ship something.",
+    ];
     const msg = greetings[Math.floor(Math.random() * greetings.length)];
     setDialogue(msg);
   }, []);
 
   return (
     <div className="home-page">
+      <div className="home-grid">
+
+        {/* ══════════════ MAIN COLUMN ══════════════ */}
+        <div className="home-main">
 
-      {/* ── STAT ROW ── */}
-      <div className="home-stats">
-        <div className="stat-card">
-          <span className="stat-value">{level}</span>
-          <span className="stat-label">Level</span>
-          <div className="stat-bar-track">
-            <div className="stat-bar-fill" style={{ width: `${levelProgress}%` }} />
+          {/* ── HERO ── */}
+          <div className="hero-panel">
+            <div className="hero-glow" aria-hidden="true" />
+            <span className="hero-kicker">// SESSION ACTIVE</span>
+            <h1 className="hero-title">
+              Welcome back, <span className="text-gradient">operator</span>.
+            </h1>
+            <p className="hero-sub">Sharp-chan has your next build queued up.</p>
+
+            <div className="home-cta">
+              {lastVisited ? (
+                <button className="cta-btn cta-btn--primary" onClick={() => navigate(`/lesson/${lastVisited}`)}>
+                  <span className="cta-btn-icon">▶</span>
+                  Continue — Lesson {lastVisited}
+                </button>
+              ) : (
+                <button className="cta-btn cta-btn--primary" onClick={() => navigate('/lesson/1.1')}>
+                  <span className="cta-btn-icon">▶</span>
+                  Begin Training
+                </button>
+              )}
+              <p className="cta-sub">
+                {totalCompleted === 0
+                  ? 'No prior experience required. Just show up.'
+                  : `${totalLessons - totalCompleted} lessons remaining.`}
+              </p>
+            </div>
           </div>
-          <span className="stat-sublabel">{xpToNextLevel} XP to next</span>
-        </div>
 
-        <div className="stat-card stat-card--xp">
-          <span className="stat-value">{xp.toLocaleString()}</span>
-          <span className="stat-label">Total XP</span>
-          <span className="stat-icon">✦</span>
-        </div>
+          {/* ── STAT HUD ── */}
+          <div className="home-stats">
+            <SpotlightCard className="stat-card">
+              <span className="stat-value">{level}</span>
+              <span className="stat-label">Level</span>
+              <div className="stat-bar-track">
+                <div className="stat-bar-fill" style={{ width: `${levelProgress}%` }} />
+              </div>
+              <span className="stat-sublabel">{xpToNextLevel} XP to next</span>
+            </SpotlightCard>
+
+            <SpotlightCard className="stat-card stat-card--xp">
+              <span className="stat-value">{xp.toLocaleString()}</span>
+              <span className="stat-label">Total XP</span>
+              <span className="stat-icon">✦</span>
+            </SpotlightCard>
 
-        <div className="stat-card">
-          <span className="stat-value">{totalCompleted}<span className="stat-value-denom">/{totalLessons}</span></span>
-          <span className="stat-label">Lessons Done</span>
-          <div className="stat-bar-track">
-            <div className="stat-bar-fill stat-bar-fill--cyan" style={{ width: `${overallPct}%` }} />
+            <SpotlightCard className="stat-card">
+              <span className="stat-value">{totalCompleted}<span className="stat-value-denom">/{totalLessons}</span></span>
+              <span className="stat-label">Lessons Done</span>
+              <div className="stat-bar-track">
+                <div className="stat-bar-fill stat-bar-fill--cyan" style={{ width: `${overallPct}%` }} />
+              </div>
+              <span className="stat-sublabel">{overallPct}% complete</span>
+            </SpotlightCard>
+
+            <SpotlightCard
+              as="div"
+              className="stat-card stat-card--shop"
+              onClick={() => navigate('/shop')}
+              role="button"
+              tabIndex={0}
+            >
+              <span className="stat-shop-icon">🛍️</span>
+              <span className="stat-label">Closet</span>
+              <span className="stat-sublabel">Lv.{level} unlocks</span>
+              <span className="stat-shop-arrow">→</span>
+            </SpotlightCard>
+          </div>
+
+          {/* ── MISSION GRID ── */}
+          <div className="home-units">
+            <h2 className="home-section-title">
+              <span className="section-title-bar" />
+              Curriculum
+            </h2>
+            <div className="unit-grid">
+              {UNITS.map((unit) => {
+                const unlocked = isUnitUnlocked(unit.id);
+                const unitCompleted = completedLessons
+                  ? Object.keys(completedLessons).filter(id => id.startsWith(`${unit.id}.`)).length
+                  : 0;
+                const unitPct = Math.round((unitCompleted / unit.lessons) * 100);
+
+                return (
+                  <SpotlightCard
+                    key={unit.id}
+                    className={`unit-card ${unlocked ? 'unit-card--active' : 'unit-card--locked'}`}
+                    onClick={() => unlocked && navigate(`/unit/${unit.id}`)}
+                    role={unlocked ? 'button' : undefined}
+                    tabIndex={unlocked ? 0 : undefined}
+                  >
+                    <div className="unit-card-head">
+                      <span className="unit-card-num">U{unit.id}</span>
+                      {unlocked ? (
+                        <span className="unit-card-pct">{unitPct}%</span>
+                      ) : (
+                        <span className="unit-card-lock">🔒</span>
+                      )}
+                    </div>
+                    <span className="unit-card-title">{unit.title}</span>
+                    <span className="unit-card-meta">{unit.lessons} lessons</span>
+                    {unlocked && (
+                      <div className="unit-card-bar-track">
+                        <div className="unit-card-bar-fill" style={{ width: `${unitPct}%` }} />
+                      </div>
+                    )}
+                  </SpotlightCard>
+                );
+              })}
+            </div>
           </div>
-          <span className="stat-sublabel">{overallPct}% complete</span>
-        </div>
 
-        <div
-          className="stat-card stat-card--shop"
-          onClick={() => navigate('/shop')}
-          role="button"
-          tabIndex={0}
-        >
-          <span className="stat-shop-icon">🛍️</span>
-          <span className="stat-label">Closet</span>
-          <span className="stat-sublabel">Lv.{level} unlocks</span>
-          <span className="stat-shop-arrow">→</span>
         </div>
-      </div>
 
-      {/* ── CTA ── */}
-      <div className="home-cta">
-        {lastVisited ? (
-          <button
-            className="cta-btn cta-btn--primary"
-            onClick={() => navigate(`/lesson/${lastVisited}`)}
-          >
-            <span className="cta-btn-icon">▶</span>
-            Continue — Lesson {lastVisited}
-          </button>
-        ) : (
-          <button
-            className="cta-btn cta-btn--primary"
-            onClick={() => navigate('/lesson/1.1')}
-          >
-            <span className="cta-btn-icon">▶</span>
-            Begin Training
-          </button>
-        )}
-        <p className="cta-sub">
-          {totalCompleted === 0
-            ? "No prior experience required. Just show up."
-            : `${totalLessons - totalCompleted} lessons remaining.`}
-        </p>
-      </div>
+        {/* ══════════════ SIDE COLUMN — MEET MY SISTERS ══════════════ */}
+        <aside className="siblings-panel">
+          <h2 className="home-section-title">
+            <span className="section-title-bar" />
+            Meet My Sisters
+          </h2>
+          <p className="siblings-intro">Same studio, different language cores. Say hi.</p>
 
-      {/* ── UNIT GRID ── */}
-      <div className="home-units">
-        <h2 className="home-section-title">
-          <span className="section-title-bar" />
-          Curriculum
-        </h2>
-        <div className="unit-grid">
-          {UNITS.map(unit => {
-            const unlocked = isUnitUnlocked(unit.id);
-            const unitCompleted = completedLessons
-              ? Object.keys(completedLessons).filter(id => id.startsWith(`${unit.id}.`)).length
-              : 0;
-            const unitPct = Math.round((unitCompleted / unit.lessons) * 100);
-
-            return (
-              <div
-                key={unit.id}
-                className={`unit-card ${unlocked ? 'unit-card--active' : 'unit-card--locked'}`}
-                onClick={() => unlocked && navigate(`/unit/${unit.id}`)}
-                role={unlocked ? 'button' : undefined}
-                tabIndex={unlocked ? 0 : undefined}
+          <div className="siblings-list">
+            {SIBLINGS.map((sib, i) => (
+              <motion.a
+                key={sib.name}
+                href={sib.self ? undefined : sib.href}
+                target={sib.self ? undefined : '_blank'}
+                rel={sib.self ? undefined : 'noopener noreferrer'}
+                className={`sibling-card ${sib.self ? 'sibling-card--self' : ''}`}
+                style={{ '--sib-hue': sib.hue }}
+                initial={{ opacity: 0, x: 12 }}
+                animate={{ opacity: 1, x: 0 }}
+                transition={{ delay: i * 0.05, duration: 0.35, ease: 'easeOut' }}
               >
-                <div className="unit-card-head">
-                  <span className="unit-card-num">U{unit.id}</span>
-                  {unlocked ? (
-                    <span className="unit-card-pct">{unitPct}%</span>
-                  ) : (
-                    <span className="unit-card-lock">🔒</span>
-                  )}
-                </div>
-                <span className="unit-card-title">{unit.title}</span>
-                <span className="unit-card-meta">{unit.lessons} lessons</span>
-                {unlocked && (
-                  <div className="unit-card-bar-track">
-                    <div className="unit-card-bar-fill" style={{ width: `${unitPct}%` }} />
-                  </div>
+                <span className="sibling-glyph">{sib.glyph}</span>
+                <span className="sibling-info">
+                  <span className="sibling-name">{sib.name}</span>
+                  <span className="sibling-lang">{sib.lang}</span>
+                </span>
+                {sib.self ? (
+                  <span className="sibling-tag">You're here</span>
+                ) : (
+                  <span className="sibling-arrow">↗</span>
                 )}
-              </div>
-            );
-          })}
-        </div>
-      </div>
+              </motion.a>
+            ))}
+          </div>
+        </aside>
 
+      </div>
     </div>
   );
 };
 
-export default Home;
\ No newline at end of file
+export default Home;
diff --git a/src/styles/globals.css b/src/styles/globals.css
index 5cbcc2d..0a49d03 100644
--- a/src/styles/globals.css
+++ b/src/styles/globals.css
@@ -15,7 +15,7 @@
   --color-border:        rgba(124, 111, 255, 0.1);
   --color-border-accent: rgba(124, 111, 255, 0.28);
 
-  /* --- Sharp-Chan Brand Colors (electric blue/violet) --- */
+  /* --- Sharp-Chan Brand Colors (neon cyberpunk: violet/cyan/magenta) --- */
   --color-primary:        #7c6fff;
   --color-primary-light:  #a99fff;
   --color-primary-dark:   #5a4fd6;
@@ -29,6 +29,23 @@
   --color-violet-light:   #d4aaff;
   --color-violet-glow:    rgba(180, 110, 255, 0.18);
 
+  /* --- Neon accents (her hair/eye shift colors) --- */
+  --color-magenta:        #ff2ecb;
+  --color-magenta-light:  #ff85e0;
+  --color-magenta-glow:   rgba(255, 46, 203, 0.22);
+
+  --color-pink:           #ff4fa3;
+  --color-pink-light:     #ff8fc4;
+  --color-pink-glow:      rgba(255, 79, 163, 0.2);
+
+  /* --- Signature gradients --- */
+  --gradient-neon:      linear-gradient(135deg, var(--color-magenta) 0%, var(--color-primary) 50%, var(--color-cyan) 100%);
+  --gradient-neon-soft: linear-gradient(135deg, rgba(255,46,203,0.15) 0%, rgba(124,111,255,0.15) 50%, rgba(100,210,255,0.15) 100%);
+  --gradient-hero:      radial-gradient(ellipse 80% 60% at 30% 0%, rgba(180,110,255,0.25) 0%, transparent 60%),
+                        radial-gradient(ellipse 70% 50% at 100% 30%, rgba(100,210,255,0.18) 0%, transparent 60%),
+                        radial-gradient(ellipse 60% 50% at 10% 100%, rgba(255,46,203,0.14) 0%, transparent 60%);
+  --gradient-text:      linear-gradient(90deg, var(--color-magenta-light), var(--color-primary-light) 45%, var(--color-cyan-light));
+
   /* --- Semantic Colors --- */
   --color-success:        #4fffb0;
   --color-success-glow:   rgba(79, 255, 176, 0.15);
@@ -76,45 +93,64 @@
 }
 
 /* ============================================
-   THEME OVERRIDES (shop-unlockable)
+   THEME OVERRIDES (shop-unlockable wallpapers)
+   Each is a cyberpunk "district" — swaps the
+   accent + gradient pairing, keeps the neon logic.
    ============================================ */
+
+/* Chrome District — cool chrome/cyan, glassy skyline */
 .theme-steel {
-  --color-primary:        #8fb8d0;
-  --color-primary-light:  #b8d4e8;
-  --color-primary-dark:   #6090aa;
-  --color-primary-glow:   rgba(143, 184, 208, 0.22);
-  --color-border-accent:  rgba(143, 184, 208, 0.28);
-  --color-xp:             #8fb8d0;
+  --color-primary:        #6ee7ff;
+  --color-primary-light:  #a8f2ff;
+  --color-primary-dark:   #2fb8d8;
+  --color-primary-glow:   rgba(110, 231, 255, 0.24);
+  --color-border-accent:  rgba(110, 231, 255, 0.3);
+  --color-xp:             #6ee7ff;
+  --color-magenta:        #8fb8ff;
+  --color-magenta-glow:   rgba(143, 184, 255, 0.2);
+  --gradient-neon:        linear-gradient(135deg, #8fb8ff 0%, #6ee7ff 50%, #b8f4ff 100%);
+  --gradient-text:        linear-gradient(90deg, #a8d4ff, #6ee7ff 50%, #d4faff);
 }
 
+/* Glitch Gold — magenta/gold circuitry, overclocked */
 .theme-indigo-gold {
-  --color-primary:        #9b6fff;
-  --color-primary-light:  #c4a0ff;
-  --color-primary-dark:   #7044dd;
-  --color-primary-glow:   rgba(155, 111, 255, 0.22);
-  --color-border-accent:  rgba(155, 111, 255, 0.28);
-  --color-xp:             #ffd700;
-  --color-cyan:           #ffd700;
-  --color-cyan-light:     #ffe566;
+  --color-primary:        #ffcc4d;
+  --color-primary-light:  #ffe08a;
+  --color-primary-dark:   #dd9a00;
+  --color-primary-glow:   rgba(255, 204, 77, 0.22);
+  --color-border-accent:  rgba(255, 204, 77, 0.3);
+  --color-xp:             #ffcc4d;
+  --color-cyan:           #ff4fd8;
+  --color-cyan-light:     #ff9bec;
+  --gradient-neon:        linear-gradient(135deg, #ff4fd8 0%, #ffcc4d 55%, #fff1b8 100%);
+  --gradient-text:        linear-gradient(90deg, #ff9bec, #ffcc4d 55%, #fff1b8);
 }
 
+/* Neon Alley — hot pink/red, rain-slicked back-street */
 .theme-crimson {
-  --color-primary:        #ff4f7b;
-  --color-primary-light:  #ff8fa8;
-  --color-primary-dark:   #cc2255;
-  --color-primary-glow:   rgba(255, 79, 123, 0.22);
-  --color-border-accent:  rgba(255, 79, 123, 0.28);
-  --color-xp:             #ff4f7b;
+  --color-primary:        #ff2e6e;
+  --color-primary-light:  #ff85ac;
+  --color-primary-dark:   #c9124a;
+  --color-primary-glow:   rgba(255, 46, 110, 0.24);
+  --color-border-accent:  rgba(255, 46, 110, 0.3);
+  --color-xp:             #ff2e6e;
+  --color-magenta:        #ff2ecb;
+  --gradient-neon:        linear-gradient(135deg, #ff2ecb 0%, #ff2e6e 55%, #ffb347 100%);
+  --gradient-text:        linear-gradient(90deg, #ff8fe0, #ff2e6e 55%, #ffb347);
 }
 
+/* Compiled — matrix green terminal, hacker channel */
 .theme-terminal {
-  --color-primary:        #4fffb0;
-  --color-primary-light:  #9fffd8;
-  --color-primary-dark:   #2acc80;
-  --color-primary-glow:   rgba(79, 255, 176, 0.22);
-  --color-border-accent:  rgba(79, 255, 176, 0.28);
-  --color-xp:             #4fffb0;
-  --color-cyan:           #4fffb0;
+  --color-primary:        #39ff8f;
+  --color-primary-light:  #9fffcf;
+  --color-primary-dark:   #12cc63;
+  --color-primary-glow:   rgba(57, 255, 143, 0.24);
+  --color-border-accent:  rgba(57, 255, 143, 0.3);
+  --color-xp:             #39ff8f;
+  --color-cyan:           #39ff8f;
+  --color-magenta:        #64d2ff;
+  --gradient-neon:        linear-gradient(135deg, #64d2ff 0%, #39ff8f 55%, #d4ffe8 100%);
+  --gradient-text:        linear-gradient(90deg, #9fe8ff, #39ff8f 55%, #d4ffe8);
 }
 
 /* ============================================
@@ -164,4 +200,65 @@ body {
 .text-success  { color: var(--color-success); }
 .text-error    { color: var(--color-error); }
 .font-code     { font-family: var(--font-code); }
-.font-display  { font-family: var(--font-display); }
\ No newline at end of file
+.font-display  { font-family: var(--font-display); }
+
+.text-gradient {
+  background: var(--gradient-text);
+  -webkit-background-clip: text;
+  background-clip: text;
+  -webkit-text-fill-color: transparent;
+}
+
+/* ============================================
+   SPOTLIGHT — cursor-reactive card glow.
+   Pair with a mousemove handler that sets
+   --spot-x / --spot-y (in px, local to the card)
+   on the element itself.
+   ============================================ */
+.spotlight {
+  position: relative;
+  --spot-x: 50%;
+  --spot-y: 50%;
+  --spot-opacity: 0;
+}
+
+.spotlight::before {
+  content: '';
+  position: absolute;
+  inset: 0;
+  border-radius: inherit;
+  background: radial-gradient(
+    420px circle at var(--spot-x) var(--spot-y),
+    var(--color-magenta-glow),
+    transparent 65%
+  );
+  opacity: var(--spot-opacity);
+  transition: opacity var(--transition-base);
+  pointer-events: none;
+  z-index: 0;
+}
+
+.spotlight::after {
+  content: '';
+  position: absolute;
+  inset: 0;
+  border-radius: inherit;
+  padding: 1px;
+  background: radial-gradient(
+    260px circle at var(--spot-x) var(--spot-y),
+    var(--color-cyan-light),
+    transparent 70%
+  );
+  opacity: var(--spot-opacity);
+  transition: opacity var(--transition-base);
+  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
+  -webkit-mask-composite: xor;
+  mask-composite: exclude;
+  pointer-events: none;
+  z-index: 1;
+}
+
+.spotlight > * {
+  position: relative;
+  z-index: 2;
+}
\ No newline at end of file
